export interface User {
    email: string | null,
    phone: string | null,
    address: string | null,
    access_token: string | null,
    user_name: string | null,
    role_id: string | null,
    img_url: string | null
}

